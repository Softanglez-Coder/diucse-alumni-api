name: Format and Push

on:
  push:
    branches:
      - main

jobs:
  format:
    runs-on: ubuntu-latest
    if: >
      github.event.head_commit.message != 'chore: format code'
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0 # Required for pushing

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install dependencies
        run: yarn install --frozen-lockfile

      - name: Run Prettier
        run: yarn format

      - name: Check for changes
        id: changes
        run: |
          if [[ -n "$(git status --porcelain)" ]]; then
            echo "has_changes=true" >> "$GITHUB_OUTPUT"
          else
            echo "has_changes=false" >> "$GITHUB_OUTPUT"
          fi

      - name: Commit and push changes
        if: >
          steps.changes.outputs.has_changes == 'true'
        env:
            GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          git config user.name "likhononair"
          git config user.email "mehedi.hasansjs@gmail.com"
          git add .
          git commit -m "chore: format code [skip ci]"
          git push